Current:
* integrate echo module's mechanism into Lua VM
* move Lua VM from global space to per-worker context
* deal with TCP 3-second delay problem under great connection harness

Future:
* add trace-back message in Lua runtime error
* add Lua VM active yield and resume (through Coco patch)
* add Lua VM passive yield and resume (using debug hook)
